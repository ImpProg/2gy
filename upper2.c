#include <stdio.h>
#include <ctype.h>

int main() {
	
	char letters[] = {'a', 'A', 'd', 'F', 'c', 'h', 't'};
	
	int i;
	for (i = 0; i < letters[i]; ++i) {
		printf("%c\n", toupper(letters[i]));
	}
	
}